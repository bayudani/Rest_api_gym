generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model cache {
  key        String @id @db.VarChar(255)
  value      String @db.MediumText
  expiration Int
}

model cache_locks {
  key        String @id @db.VarChar(255)
  owner      String @db.VarChar(255)
  expiration Int
}

model failed_jobs {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String   @unique(map: "failed_jobs_uuid_unique") @db.VarChar(255)
  connection String   @db.Text
  queue      String   @db.Text
  payload    String   @db.LongText
  exception  String   @db.LongText
  failed_at  DateTime @default(now()) @db.Timestamp(0)
}

model fblog_categories {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @unique(map: "fblog_categories_name_unique") @db.VarChar(155)
  slug       String    @unique(map: "fblog_categories_slug_unique") @db.VarChar(155)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model fblog_category_fblog_post {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  post_id     BigInt    @db.UnsignedBigInt
  category_id BigInt    @db.UnsignedBigInt
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)

  @@index([category_id], map: "fblog_category_fblog_post_category_id_foreign")
  @@index([post_id], map: "fblog_category_fblog_post_post_id_foreign")
}

model fblog_comments {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id     BigInt    @db.UnsignedBigInt
  post_id     BigInt    @db.UnsignedBigInt
  comment     String    @db.Text
  approved    Boolean   @default(false)
  approved_at DateTime? @db.DateTime(0)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)

  @@index([post_id], map: "fblog_comments_post_id_foreign")
}

model fblog_news_letters {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  email      String    @unique(map: "fblog_news_letters_email_unique") @db.VarChar(100)
  subscribed Boolean   @default(true)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model fblog_post_fblog_tag {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  post_id    BigInt    @db.UnsignedBigInt
  tag_id     BigInt    @db.UnsignedBigInt
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@index([post_id], map: "fblog_post_fblog_tag_post_id_foreign")
  @@index([tag_id], map: "fblog_post_fblog_tag_tag_id_foreign")
}

model fblog_posts {
  id               BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  title            String             @db.VarChar(255)
  slug             String             @db.VarChar(255)  @unique
  sub_title        String?            @db.VarChar(255)
  body             String             @db.LongText
  status           fblog_posts_status @default(pending)
  published_at     DateTime?          @db.DateTime(0)
  scheduled_for    DateTime?          @db.DateTime(0)
  cover_photo_path String             @db.VarChar(255)
  photo_alt_text   String             @db.VarChar(255)
  user_id          BigInt             @db.UnsignedBigInt
  created_at       DateTime?          @db.Timestamp(0)
  updated_at       DateTime?          @db.Timestamp(0)

  @@index([user_id], map: "fblog_posts_user_id_foreign")
}

model fblog_seo_details {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  post_id     BigInt    @db.UnsignedBigInt
  title       String    @db.VarChar(255)
  keywords    Json?
  description String    @db.Text
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)

  @@index([post_id], map: "fblog_seo_details_post_id_foreign")
}

model fblog_settings {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  title                String?   @db.VarChar(155)
  description          String?   @db.Text
  logo                 String?   @db.VarChar(255)
  favicon              String?   @db.VarChar(255)
  organization_name    String?   @db.VarChar(255)
  google_console_code  String?   @db.TinyText
  google_analytic_code String?   @db.Text
  google_adsense_code  String?   @db.TinyText
  quick_links          Json?
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
}

model fblog_share_snippets {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  script_code String    @db.LongText
  html_code   String    @db.Text
  active      Boolean   @default(true)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
}

model fblog_tags {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @unique(map: "fblog_tags_name_unique") @db.VarChar(50)
  slug       String    @unique(map: "fblog_tags_slug_unique") @db.VarChar(155)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model job_batches {
  id             String  @id @db.VarChar(255)
  name           String  @db.VarChar(255)
  total_jobs     Int
  pending_jobs   Int
  failed_jobs    Int
  failed_job_ids String  @db.LongText
  options        String? @db.MediumText
  cancelled_at   Int?
  created_at     Int
  finished_at    Int?
}

model jobs {
  id           BigInt @id @default(autoincrement()) @db.UnsignedBigInt
  queue        String @db.VarChar(255)
  payload      String @db.LongText
  attempts     Int    @db.UnsignedTinyInt
  reserved_at  Int?   @db.UnsignedInt
  available_at Int    @db.UnsignedInt
  created_at   Int    @db.UnsignedInt

  @@index([queue], map: "jobs_queue_index")
}

model migrations {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  migration String @db.VarChar(255)
  batch     Int
}

model model_has_permissions {
  permission_id BigInt @db.UnsignedBigInt
  model_type    String @db.VarChar(255)
  model_id      BigInt @db.UnsignedBigInt

  @@id([permission_id, model_id, model_type])
  @@index([model_id, model_type], map: "model_has_permissions_model_id_model_type_index")
}

model model_has_roles {
  role_id    BigInt @db.UnsignedBigInt
  model_type String @db.VarChar(255)
  model_id   BigInt @db.UnsignedBigInt

  @@id([role_id, model_id, model_type])
  @@index([model_id, model_type], map: "model_has_roles_model_id_model_type_index")
}

model password_reset_tokens {
  email      String    @id @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
}

model permissions {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @db.VarChar(255)
  guard_name String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@unique([name, guard_name], map: "permissions_name_guard_name_unique")
}

model personal_access_tokens {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  tokenable_type String    @db.VarChar(255)
  tokenable_id   BigInt    @db.UnsignedBigInt
  name           String    @db.VarChar(255)
  token          String    @unique(map: "personal_access_tokens_token_unique") @db.VarChar(64)
  abilities      String?   @db.Text
  last_used_at   DateTime? @db.Timestamp(0)
  expires_at     DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)

  @@index([tokenable_type, tokenable_id], map: "personal_access_tokens_tokenable_type_tokenable_id_index")
}

model post_likes {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  post_id    BigInt    @db.UnsignedBigInt
  user_id    BigInt    @db.UnsignedBigInt
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@unique([post_id, user_id], map: "post_likes_post_id_user_id_unique")
  @@index([user_id], map: "post_likes_user_id_foreign")
}

model role_has_permissions {
  permission_id BigInt @db.UnsignedBigInt
  role_id       BigInt @db.UnsignedBigInt

  @@id([permission_id, role_id])
  @@index([role_id], map: "role_has_permissions_role_id_foreign")
}

model roles {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @db.VarChar(255)
  guard_name String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@unique([name, guard_name], map: "roles_name_guard_name_unique")
}

model sessions {
  id            String  @id @db.VarChar(255)
  user_id       BigInt? @db.UnsignedBigInt
  ip_address    String? @db.VarChar(45)
  user_agent    String? @db.Text
  payload       String  @db.LongText
  last_activity Int

  @@index([last_activity], map: "sessions_last_activity_index")
  @@index([user_id], map: "sessions_user_id_index")
}

model users {
  id                 BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name               String    @db.VarChar(255)
  email              String    @unique(map: "users_email_unique") @db.VarChar(255)
  email_verified_at  DateTime? @db.Timestamp(0)
  password           String    @db.VarChar(255)
  remember_token     String?   @db.VarChar(100)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  profile_photo_path String?   @db.VarChar(255)
  is_verified       Boolean   @default(false)
  verification_code String?   // nullable
}

model memberships {
  id              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name            String    @db.VarChar(50)
  duration_months Int
  price           Decimal   @default(0.00) @db.Decimal(8, 2)
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
}

model attends {
  id                BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  member_profile_id BigInt    @db.UnsignedBigInt
  scan_time         DateTime  @db.Timestamp(0)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)

  @@index([member_profile_id], map: "attends_member_profile_id_foreign")
}

model member_profiles {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt    @db.UnsignedBigInt @unique
  full_name  String    @db.VarChar(50)
  addres     String?   @db.VarChar(100)
  phone      String?   @db.VarChar(20)
  is_active  Boolean   @default(false)
  start_date DateTime? @db.Date
  end_date   DateTime? @db.Date
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  point      Int       @default(0)


  @@index([user_id], map: "member_profiles_user_id_foreign")
  rewardss   rewardss[]  // ini penting buat bisa di-`include`

}

model transactions {
  id                    BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  userId                BigInt              @db.UnsignedBigInt
  membership_package_id BigInt              @db.UnsignedBigInt
  amount                Int
  proof_image           String?             @db.VarChar(255)
  status                transactions_status @default(pending)
  created_at            DateTime?           @db.Timestamp(0)
  updated_at            DateTime?           @db.Timestamp(0)

  @@index([membership_package_id], map: "transactions_membership_package_id_foreign")
  @@index([userId], map: "transactions_userid_foreign")
}

model daily_players {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model item_rewards {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @db.VarChar(100)
  points     Int       @default(0)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  // Relasi: Satu item reward bisa diklaim berkali-kali oleh member berbeda
}

model rewardss {
  id                BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  member_profile_id BigInt                 @db.UnsignedBigInt @unique
  item_reward_id    BigInt                 @db.UnsignedBigInt
  reward_status     rewardss_reward_status @default(pending)
  created_at        DateTime?              @db.Timestamp(0)
  updated_at        DateTime?              @db.Timestamp(0)

  @@index([item_reward_id], map: "rewardss_item_reward_id_foreign")
  @@index([member_profile_id], map: "rewardss_member_profile_id_foreign")

   // FIX: Definisikan relasi secara eksplisit (Ini yang hilang)

   member_profile member_profiles @relation(fields: [member_profile_id], references: [id])

}


model programs {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name        String    @db.VarChar(255)
  slug        String    @db.VarChar(255)
  description String?   @db.VarChar(255)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
}

enum fblog_posts_status {
  published
  scheduled
  pending
}

enum transactions_status {
  pending
  Confirmed
}

enum rewardss_reward_status {
  pending
  claimed
}
